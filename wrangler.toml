name = "mercy-ships-tracker"
main = "src/worker.js"
compatibility_date = "2024-09-23"

# Optional: add a custom domain in the Cloudflare dashboard after deploying
# (e.g. tracker.mercyships.org via Workers Routes)
